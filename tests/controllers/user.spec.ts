describe('User Controller', () => {
  describe('index', () => {
    it('should show all users when accesing user route', () => {
      expect.assertions(1);
      
      expect(1).toEqual(1);
    });
  });
  describe('show', () => { });
  describe('store', () => { });
  describe('update', () => { });
  describe('delete', () => { });
});